= simple_form_for @cliente, :html => { :class => 'form-horizontal' } do |f|
  = f.input :nome
  = f.error_span :nome
  = f.input :telefone
  = f.error_span :telefone
  = f.input :endereco
  = f.error_span :endereco
  = f.input :cpf
  = f.error_span :cpf
  = f.button :submit, :class => 'btn-primary'
  = link_to t('.cancel', :default => t("helpers.links.cancel")), clientes_path, :class => 'btn btn-default'
